# Home-server blog API environment
HOST=127.0.0.1
PORT=8787
SQLITE_PATH=./data/blog.sqlite

# Public frontend origin that should be allowed by CORS is currently coded as
# https://xmin.blog plus localhost dev origins.

# GitHub repo used for post markdown and uploaded image commits.
GITHUB_OWNER=xmin-02
GITHUB_REPO=xminblog
GITHUB_BRANCH=main
GITHUB_TOKEN=ghp_replace_me

# Admin login: admin@xmin.blog + ADMIN_PASSWORD
ADMIN_PASSWORD=replace-with-long-random-password
JWT_SECRET=replace-with-32-byte-random-secret
