---
title: "[리눅스 커널 취약점 분석] 리눅스 커널 퍼징용 커널 빌드"
description: "리눅스 커널 퍼징을 위한 KCOV, KASAN 등 디버깅 옵션을 활성화한 커널 빌드 과정"
date: 2025-11-01T01:52:00+09:00
category: "환경 구축"
tags: ["fuzz", "Fuzzing", "Kernel", "Linux Kernel", "Syzkaller"]
draft: false
---

# [리눅스 커널 취약점 분석] 리눅스 커널 퍼징용 커널 빌드

본 블로그는 로컬 환경에서 실시되었으며, wsl과 로컬 환경 동일합니다.

linux-6.17.6 버전을 기준으로 작성 되었으며, 모든 설치 과정은 동일하니 linux-6.17.6 부분만 각자 다운받은 버전으로 변경하면 됩니다.

## Kernel Source Download

먼저 아래 링크에서 커널 소스를 다운 받는다.

[https://kernel.org/](https://kernel.org/)

```bash
sudo mkdir /usr/src/kernel
cd /usr/src/kernel
sudo cp ~/Downloads/linux-linux-6.17.6.tar.xz ./
```

## Extract and Configure

```bash
sudo su
tar -xvf linux-6.17.6.tar.xz
```

## Dependency Installation

```bash
sudo apt update
sudo apt install -y build-essential libncurses-dev bison flex libssl-dev libelf-dev dwarves libdw-dev libdwarf-dev zlib1g-dev libiberty-dev bc fakeroot cpio kmod rsync curl clang llvm lld gawk
```

## Fuzzing Configuration Options

```bash
make menuconfig

scripts/config --disable SYSTEM_TRUSTED_KEYS
scripts/config --disable SYSTEM_REVOCATION_KEYS
scripts/config --disable SYSTEM_BLACKLIST_KEYRING
scripts/config --set-str SYSTEM_BLACKLIST_HASH_LIST ""

scripts/config --enable KCOV
scripts/config --enable KCOV_INSTRUMENT_ALL
scripts/config --enable KCOV_ENABLE_COMPARISONS

scripts/config --enable KASAN
scripts/config --set-str KASAN_MODE "inline"

scripts/config --enable KMSAN
scripts/config --enable KMSAN_KCOV

scripts/config --enable UBSAN
scripts/config --enable UBSAN_SANITIZE_ALL

scripts/config --enable PROVE_LOCKING
scripts/config --enable DEBUG_LOCK_ALLOC
scripts/config --enable LOCKDEP

scripts/config --enable DEBUG_INFO
scripts/config --enable DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT
scripts/config --enable DEBUG_KERNEL
scripts/config --enable DEBUG_FS
scripts/config --enable GDB_SCRIPTS

scripts/config --disable CONFIG_DEBUG_INFO_REDUCED
scripts/config --disable CONFIG_LTO_CLANG
scripts/config --disable CONFIG_STACK_VALIDATION
scripts/config --enable CONFIG_CONFIGFS_FS

make olddefconfig
```

## Build Process

```bash
make -j$(nproc)
```

![커널 빌드 성공 출력](https://blog.kakaocdn.net/dna/bxz1KN/dJMcac2sOfB/AAAAAAAAAAAAAAAAAAAAAJjeaQ-b-JYLkImrC0sSy26WQN_1cq7XroDqDPvWO6nG/img.png?credential=yqXZFxpELC7KVnFOS48ylbz2pIh7yKj8&expires=1777561199&allow_ip=&allow_referer=&signature=VRwDbw252iXlHf0WF4L8X9sX2YM%3D)

"Kernel: arch/x86/boot/bzImage is ready" 문구가 나온다면 성공한 것이다.

## Module Installation

```bash
make modules
sudo make modules_install
sudo make install
```

![모듈 설치 출력](https://blog.kakaocdn.net/dna/IXjqM/dJMcabJfKLR/AAAAAAAAAAAAAAAAAAAAAAiQyU70s8ulGWsAT7L8NzXLQFZB1MpxZ07akgQcvlue/img.png?credential=yqXZFxpELC7KVnFOS48ylbz2pIh7yKj8&expires=1777561199&allow_ip=&allow_referer=&signature=2V%2FsFqL1F6btkZ6jaK2YMBOnK84%3D)

![vmlinuz-6.17.6 확인](https://blog.kakaocdn.net/dna/4cvuu/dJMcagKyPXM/AAAAAAAAAAAAAAAAAAAAAMIdnC8GPATvxxiW153BveSczPtHWg7A82GnRgd8Ydrg/img.png?credential=yqXZFxpELC7KVnFOS48ylbz2pIh7yKj8&expires=1777561199&allow_ip=&allow_referer=&signature=p5QbKkfHY7HQxIpODpRQlJNExic%3D)

다음과 같이 vmlinuz-6.17.6가 존재 한다는 것을 알 수 있다. 잘 빌드가 된 것이다. 이제 이 커널로 부팅이 가능하다.

