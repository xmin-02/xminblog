---
import Layout from './Layout.astro';

interface Props {
  title: string;
  description?: string;
  canonical?: string;
  noindex?: boolean;
  ogType?: string;
  active?: number;
  path?: string;
  progress?: boolean;
}

const {
  title,
  description = 'xmin의 개인 블로그 — 보안, AI, 개발',
  canonical,
  noindex = false,
  ogType = 'website',
  active,
  path = '',
  progress = false,
} = Astro.props;
---

<Layout
  title={title}
  description={description}
  canonical={canonical}
  noindex={noindex}
  ogType={ogType}
  active={active}
  path={path}
  progress={progress}
  bare={true}
>
  <slot />
</Layout>
